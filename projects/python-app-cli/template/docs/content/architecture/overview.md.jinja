# Architecture

High-level design notes for `{{ project_slug }}`.

CLI entry: `src/{{ project_slug }}/cli.py` (argparse). Swap or branch to click / clack later as needed.

Add ADRs or diagrams under this folder as the project evolves.
