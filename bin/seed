#!/usr/bin/env bash
# seed — project scaffolds (Copier via ut_cli)
set -euo pipefail
ROOT="$(cd "$(dirname "${BASH_SOURCE[0]}")/.." && pwd)"
export PYTHONPATH="${ROOT}${PYTHONPATH:+:$PYTHONPATH}"
exec python -m ut_cli.seed_cmd "$@"
